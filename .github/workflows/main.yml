name: first workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: output msg
        run: echo "hello from workflow"

      - name: checkout repo
        uses: actions/checkout@v6.1.0
        
