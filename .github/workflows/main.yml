name: first workflow

on:
  push:
    branches:
      - main

jobs:
  example-job:
    runs-on: ubuntu-latest

    steps:
      - name: output msg
        run: echo "hello from workflow"

      - name: checkout repo
        uses: actions/checkout@v6.1.0

  server-resourses:
    runs-on: ubuntu-latest

    steps:
      - name: view files
        run: ls -la
        
